name: Release Please

on:
  push:
    branches: ['main']

env:
  # Bootstrap reference for the new Docker Hub repository. After the first
  # ci-base.yml run, replace latest with the immutable sha-* tag from its summary.
  # Keep this in sync with the test job's container.image.
  CI_BASE_IMAGE: docker.io/wesdobry/tubeless-ci-base:latest

jobs:
  test:
    name: Lint and Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # Run every step natively inside the configured ci-base image. GitHub mounts
    # the workspace into the container, so no compose, no detached keepalive, and
    # no `exec -T` TTY workaround are needed.
    container:
      image: docker.io/wesdobry/tubeless-ci-base:latest
    env:
      MIX_ENV: test

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # Some tests and fixtures hardcode /app (the historical compose bind-mount
      # point) — e.g. paths baked into test/support/files/*.json and the 101/100
      # exit-code mock scripts. Point /app at the checked-out workspace so those
      # absolute paths resolve under the container: runner without touching code.
      - name: Symlink workspace to /app
        run: |
          rm -rf /app
          ln -s "$GITHUB_WORKSPACE" /app

      - name: Cache deps and build
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            deps
            _build
            node_modules
            assets/node_modules
          key: ${{ runner.os }}-mix-${{ hashFiles('mix.lock', 'mix.exs', 'yarn.lock', 'assets/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-

      - name: Install Elixir and JS deps
        run: |
          mix deps.get
          yarn install
          (cd assets && yarn install)

      # SQLean SQLite extensions aren't committed to git (fetched at build time).
      # Migrations use gen_random_uuid() and queries use regexp_like(), so they
      # must be present before ecto.create/migrate and the test run.
      - name: Fetch SQLean extensions
        run: ./tooling/fetch-sqlean.sh

      - name: Create and Migrate database
        run: |
          mix ecto.create
          mix ecto.migrate

      - name: Run code checks
        run: mix check --no-fix --no-retry

  release-please:
    name: Release Please
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: write
      pull-requests: write
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
      html_url: ${{ steps.release.outputs.html_url }}
      body: ${{ steps.release.outputs.body }}
    steps:
      - uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5
        id: release
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  docker:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: release-please
    if: needs.release-please.outputs.release_created == 'true'
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Strip v prefix from release tag
        id: version
        run: |
          echo "value=$(echo '${{ needs.release-please.outputs.tag_name }}' | sed 's/^v//')" >> $GITHUB_OUTPUT

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: |
            docker.io/wesdobry/tubeless
          tags: |
            type=raw,value=${{ steps.version.outputs.value }}
            type=raw,value=latest

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Login to Docker Hub
        uses: docker/login-action@abd2ef45e78c5afb21d64d4ca52ee8550d9572c7 # v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: ./docker/selfhosted.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: CI_BASE_IMAGE=${{ env.CI_BASE_IMAGE }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  github-releases-to-discord:
    name: Publish Release Notification to Discord
    runs-on: ubuntu-latest
    needs: [release-please, docker]
    if: needs.release-please.outputs.release_created == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: GitHub Releases to Discord
        uses: SethCohen/github-releases-to-discord@24d166886aee4646d448c8a389ff9e1ebcab3682 # v1
        with:
          webhook_url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          color: '2105893'
          username: 'Releases'
          avatar_url: 'https://cdn.discordapp.com/avatars/670096391862747159/e57fd67dc7ca0cc840a0e87a82281bc5.png?size=4096'
          content: '<@&1529575590557126666>'
          footer_title: 'Releases'
          reduce_headings: true
          release_name: ${{ needs.release-please.outputs.tag_name }}
          release_body: ${{ needs.release-please.outputs.body }}
          release_html_url: ${{ needs.release-please.outputs.html_url }}
